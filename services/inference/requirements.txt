fastapi>=0.100.0
uvicorn>=0.22.0
mlflow==2.11.3
setuptools<81
redis>=4.6.0
pandas>=2.0.0
numpy>=1.24.0
# Runtime cho các flavor model phục vụ từ MLflow Registry (pin khớp services/training)
xgboost==2.1.4
scikit-learn==1.7.2
statsmodels>=0.14.0
torch>=2.0.0
# shared is installed separately (see Dockerfile or `pip install -e ./shared` from repo root)
