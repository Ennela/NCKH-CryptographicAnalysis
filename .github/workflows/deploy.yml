name: Continuous Deployment

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: actions/setup-qemu-action@v2

      - name: Set up QEMU
        uses: actions/setup-buildx-action@v2

      - name: Login to DockerHub / GitHub Packages
        run: |
          echo "TODO: Add docker login credentials using GitHub Secrets"
          # echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build and Push Ingestion Service
        run: |
          echo "Building ingestion image..."
          # docker build -t your-registry/forecast-ingestion:latest -f services/ingestion/Dockerfile .
          # docker push your-registry/forecast-ingestion:latest

      - name: Build and Push Inference Service
        run: |
          echo "Building inference image..."
          # docker build -t your-registry/forecast-inference:latest -f services/inference/Dockerfile .
          # docker push your-registry/forecast-inference:latest

      - name: Build and Push Training Service
        run: |
          echo "Building training image..."
          # docker build -t your-registry/forecast-training:latest -f services/training/Dockerfile .
          # docker push your-registry/forecast-training:latest

      - name: Build and Push Frontend
        run: |
          echo "Building frontend image..."
          # docker build -t your-registry/forecast-frontend:latest ./frontend
          # docker push your-registry/forecast-frontend:latest

  deploy:
    name: Deploy to Production VM
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Deploy via SSH
        run: |
          echo "TODO: SSH into the server and run docker-compose pull && docker-compose up -d"
          # uses: appleboy/ssh-action@master
          # with:
          #   host: ${{ secrets.PROD_HOST }}
          #   username: ${{ secrets.PROD_USER }}
          #   key: ${{ secrets.PROD_SSH_KEY }}
          #   script: |
          #     cd /home/ubuntu/stock-crypto-forecast
          #     git pull origin main
          #     docker-compose -f docker-compose.prod.yml pull
          #     docker-compose -f docker-compose.prod.yml up -d --remove-orphans
